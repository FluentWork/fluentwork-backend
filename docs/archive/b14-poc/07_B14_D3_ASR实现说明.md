# B14 D3/T2 实现说明：duplex 音频上行与 ASR 文本（V1）

**对应 issue**：`B14`  
**对应计划**：D3 / 用例 T2  
**日期**：2026-08-30  
**状态**：live smoke PASS（`v1_asr_text_ok=true`）  
**前置**：`docs/06_B14_D2_duplex实现说明.md`  
**执行清单**：`docs/04_B14_注入POC执行清单.md`

---

## 一、交付结论

| 验证项 | 结果 |
|---|---|
| V1：用户话轮 ASR 文本 | **PASS** — 收到 `conversation.item.input_audio_transcription.*` |
| 音频格式 | 16 kHz mono PCM s16le，20ms chunk（640 bytes）上行 |
| 强制尾点 | `input_audio_buffer.commit` |
| 可否冻结 B12 | **否** — 仍缺 V3/V5 注入生效与 T9 窗口 |

---

## 二、实现要点

1. `SendPCM` / `CommitAudio` / `SendUserPCMAndWait`（`volc_duplex.go`）  
2. `LoadWAVPCM16LE` 解析含 `FLLR` 等非标准 chunk 的 WAV  
3. 夹具：`internal/voicepoc/testdata/cache_invalidation_16k.wav`（macOS `say` + `afconvert`）  
4. CLI：`./scripts/smoke-volc-realtime.sh --asr`  

ASR `delta` 事件按**整段假设替换**处理（勿字符串拼接），否则会出现 `todaytoday…` 粘连。

---

## 三、命令

```bash
# 需 proxy_on（若本机依赖代理）
./scripts/smoke-volc-realtime.sh --asr
go test ./internal/voicepoc/...
```

---

## 四、下一 ticket

T3/T4：ASR 完成后（或 VAD 附近）`session.update` 注入，检查 `response.output_text.*` 是否出现确认行为（V3/V5 初测）。
