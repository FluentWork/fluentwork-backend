# B14 注入 POC 执行清单

**对应 issue**：`B14`  
**上游**：`fluentwork-meta`《50_FluentWork端到端注入能力验证文档》  
**实现说明（D2）**：`docs/06_B14_D2_duplex实现说明.md`  
**实现说明（D3/T2）**：`docs/07_B14_D3_ASR实现说明.md`  
**实现说明（T3/T4）**：`docs/08_B14_T3T4_注入生效实现说明.md`  
**状态**：V1/V2 PASS；V3 **下轮** PASS、**同轮**未过（档位②线索）；**T9 窗口与 B7 档位仍未冻结**

---

## 一、当前已具备

1. `internal/voicepoc`：T9 窗口统计 + mock/live duplex adapter  
2. **D2** `session.create` / `session.update`（V2）  
3. **D3/T2** ASR 文本事件（V1）+ PCM 夹具  
4. **T3/T4** `--inject`：同轮无效、下轮 `INJECT_OK`（V3 下轮 / 档位②线索）  
5. `cmd/poc-injection-window`：mock T9；有 Key 时附带 D2  

```bash
go test ./internal/voicepoc/...
./scripts/smoke-volc-realtime.sh            # D2（V2）
./scripts/smoke-volc-realtime.sh --asr      # D3/T2（V1）
./scripts/smoke-volc-realtime.sh --inject   # T3/T4（V3 同轮/下轮）
./scripts/poc-injection-window.sh
```

---

## 二、冻结 B12 前仍缺

| 项 | 说明 |
|---|---|
| meta #12 PREREQ | 额度算账 / 协议条款 / 并发配额 |
| T9 延迟窗口 | 量化同轮是否偶发可行；冻结 ①/②/③ |
| 回写 `meta` doc 50 | 结论 A/B/C + B7 档位 |

纪律：mock / 单次 `--inject` **不能**单独作为 `B12` 启动门禁。

```bash
./scripts/smoke-volc-ark.sh
./scripts/smoke-volc-speech-creds.sh
./scripts/smoke-volc-realtime.sh --inject
```

---

## 三、执行顺序（对齐 doc 50 第六章）

1. D1：凭证 — Key 已齐；商务三项仍缺  
2. **D2 — DONE（V2）**  
3. **D3/T2 — DONE（V1）**；**T3/T4 — DONE（V3 下轮；同轮未过）**  
4. **下一步 D5**：T5/T8/T9 → V8 窗口 + B7 档位冻结  
5. D6-D7：V6/V7 备选与基线  
6. D8：汇总回写 meta → Go/No-Go  

---

## 四、环境变量

- `VOLC_SPEECH_API_KEY` / `VOLC_POC_API_KEY`（必填，live）
- `VOLC_POC_ENDPOINT`（可选）
- `VOLC_POC_LIVE_T9=1`（可选，通道延迟探针）
