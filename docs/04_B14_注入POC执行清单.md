# B14 注入 POC 执行清单

**对应 issue**：`B14`  
**上游**：`fluentwork-meta`《50_FluentWork端到端注入能力验证文档》  
**实现说明（D2）**：`docs/06_B14_D2_duplex实现说明.md`  
**实现说明（D3/T2）**：`docs/07_B14_D3_ASR实现说明.md`  
**状态**：D2（V2 注入通道）+ D3/T2（V1 ASR 文本）live PASS；**T9 同轮观测与 B7 档位仍未冻结**

---

## 一、当前已具备

1. `internal/voicepoc`：T9 延迟梯度、窗口 P50/P90、三档映射（①/②/③）
2. `MockInjectionProvider`：无凭证可跑通管线与单测
3. **Live**：`VolcDuplexInjectionProvider` + `OpenDuplex`（官方全双工 JSON WSS，仅 `X-Api-Key`）
4. `cmd/smoke-volc-realtime` / `scripts/smoke-volc-realtime.sh`：**D2**（V2）+ **`--asr` D3/T2**（V1）
5. `testdata/cache_invalidation_16k.wav`：16 kHz mono PCM 夹具（macOS `say`/`afconvert` 生成）
6. `cmd/poc-injection-window`：有语音 Key 时自动跑 D2；`VOLC_POC_LIVE_T9=1` 时跑 live 通道延迟探针

```bash
go test ./internal/voicepoc/...
./scripts/smoke-volc-realtime.sh          # D2 live（V2）
./scripts/smoke-volc-realtime.sh --asr    # D3/T2 live（V1 ASR 文本）
./scripts/poc-injection-window.sh
```

---

## 二、冻结 B12 前仍缺

| 项 | 说明 |
|---|---|
| meta #12 PREREQ | 额度算账 / 协议条款 / 并发配额 |
| T9 同轮音频观测 | 当前 live 探针只证明 `session.update` 在延迟后可下发，**不能**当同轮生效窗口证据 |
| 回写 `meta` doc 50 | 附录验证报告：结论 A/B/C + B7 档位 ①/②/③ |

纪律：mock 报告与「仅通道探针」**不能**作为 `B12` 启动门禁证据。

火山侧应开通哪些产品 / 应用 / 模型：见 `fluentwork-meta/docs/30_技术方案/34_FluentWork火山引擎选型与开通清单.md`。

```bash
./scripts/smoke-volc-ark.sh          # 方舟文本
./scripts/smoke-volc-speech-creds.sh # 语音 Key（TTS HTTP）
./scripts/smoke-volc-realtime.sh     # 实时 duplex WSS（B14 D2）
```

---

## 三、执行顺序（对齐 doc 50 第六章）

1. D1：凭证与试用额度（#12）— 语音 Key 已齐；商务三项仍缺  
2. **D2：端到端最小会话打通 — DONE（duplex / V2）**  
3. **D3/T2：ASR 文本事件 — DONE（V1）**；下一步 T3/T4（注入后同轮行为 V3/V5）  
4. D5：T5/T8/T9 → **V8 窗口 + B7 档位**  
5. D6-D7：V6/V7 备选与基线  
6. D8：汇总回写 meta → Go/No-Go 输入件  

---

## 四、本仓命令

```bash
./scripts/smoke-volc-realtime.sh
./scripts/poc-injection-window.sh
```

环境变量：

- `VOLC_SPEECH_API_KEY` / `VOLC_POC_API_KEY`（必填，live）
- `VOLC_POC_ENDPOINT`（可选，默认 duplex WSS）
- `VOLC_POC_LIVE_T9=1`（可选，live 通道延迟探针）
