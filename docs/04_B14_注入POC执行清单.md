# B14 注入 POC 执行清单

**对应 issue**：`B14`  
**上游**：`fluentwork-meta`《50_FluentWork端到端注入能力验证文档》  
**状态**：脚手架已落地；**live 档位结论未冻结**（缺火山凭证 + live adapter）

---

## 一、当前已具备

1. `internal/voicepoc`：T9 延迟梯度、窗口 P50/P90、三档映射（①/②/③）
2. `MockInjectionProvider`：无凭证可跑通管线与单测
3. `cmd/poc-injection-window`：一键输出 JSON 报告（明确标注 mock 不可冻结 B12）

```bash
go test ./internal/voicepoc/...
go run ./cmd/poc-injection-window
# or
./scripts/poc-injection-window.sh
```

---

## 二、冻结 B12 前仍缺

| 项 | 说明 |
|---|---|
| meta #12 PREREQ | 额度算账 / 协议条款 / 并发配额 |
| `VOLC_POC_ENDPOINT` + `VOLC_POC_API_KEY` | 直连火山 POC 凭证 |
| `InjectionProvider` live adapter | 真实会话 + 注入 API，不得把火山类型泄漏进 gateway 主流程 |
| 回写 `meta` doc 50 | 附录验证报告：结论 A/B/C + B7 档位 ①/②/③ |

纪律：mock 报告**不能**作为 `B12` 启动门禁证据。

火山侧应开通哪些产品 / 应用 / 模型：见 `fluentwork-meta/docs/30_技术方案/34_FluentWork火山引擎选型与开通清单.md`。

方舟文本 Endpoint 连通性：

```bash
cp configs/volc.env.example .env.volc.local   # 填方舟 API Key（不是语音 Key）
set -a && source .env.volc.local && set +a
./scripts/smoke-volc-ark.sh
```

---

## 三、执行顺序（对齐 doc 50 第六章）

1. D1：凭证与试用额度（#12）
2. D2：端到端最小会话打通
3. D3-D4：T2/T3/T4（V1-V3、V5）
4. D5：T5/T8/T9 → **V8 窗口 + B7 档位**
5. D6-D7：V6/V7 备选与基线
6. D8：汇总回写 meta → Go/No-Go 输入件

---

## 四、本仓命令

```bash
./scripts/poc-injection-window.sh
```

可选环境变量（live 就绪后）：

- `VOLC_POC_ENDPOINT`
- `VOLC_POC_API_KEY`
