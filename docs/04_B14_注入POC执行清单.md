# B14 注入 POC 执行清单

**对应 issue**：`B14`  
**上游**：`fluentwork-meta`《50_FluentWork端到端注入能力验证文档》  
**实现说明**：`docs/06` D2 · `docs/07` D3/ASR · `docs/08` T3/T4 · `docs/09` T9  
**状态**：live V1/V2/V3(下轮)/V8 已跑通 → **B7 档位候选 ②**；#12 未闭环前非正式生产冻结

---

## 一、当前已具备

1. duplex live：`OpenDuplex` / ASR / `session.update` / T9  
2. smokes：`--asr` · `--inject` · `--t9`  
3. mock T9 管线（`poc-injection-window`）  
4. meta doc 50 已回写档位②候选  

```bash
go test ./internal/voicepoc/...
./scripts/smoke-volc-realtime.sh          # D2 V2
./scripts/smoke-volc-realtime.sh --asr    # V1
./scripts/smoke-volc-realtime.sh --inject # V3 same/next
./scripts/smoke-volc-realtime.sh --t9     # V8 → tier ②
# VOLC_T9_TRIALS=6 ./scripts/smoke-volc-realtime.sh --t9
```

---

## 二、冻结 B12 前仍缺

| 项 | 说明 |
|---|---|
| meta #12 PREREQ | 额度 / 不训练 / 并发 |
| （可选）加大 T9 样本 | `VOLC_T9_TRIALS=6` |
| V4/V5 大样本、V6/V7 | 若要冲当轮①再开 |

---

## 三、执行顺序

1. D1 Key — DONE；商务三项仍缺  
2. D2 V2 — DONE  
3. D3/T2 V1 + T3/T4 — DONE  
4. **D5 T9 — DONE（档位②候选）**  
5. D6-D7 / D8 — 按需；#12 闭环后把「候选」升为生产冻结  

---

## 四、环境变量

- `VOLC_SPEECH_API_KEY` / `VOLC_POC_API_KEY`
- `VOLC_POC_ENDPOINT` / `VOLC_DUPLEX_MODEL` / `VOLC_DUPLEX_VOICE`
- `VOLC_T9_TRIALS`（默认 1）
