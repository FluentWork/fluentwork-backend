# 第二波残留项登记

## 目的

记录截至 2026-08-31 已确认但尚未实现的缺失项，避免在 `B10` / `B11` / `B12` 推进过程中遗忘。

## 已收口项

1. `B8-R1`
   - 原先担心 `review.eval / review.refine` 拆账未完成
   - 现已明确：当前 review + refine 共用**一次** Ark 调用
   - 因此 `ai_cost_logs` 只写一条 `review.eval`
   - 不再单独追 `review.refine` residual

## 仍未开始项

1. `voice.hit_detect`
   - 对应 issue：`B12`
   - 约束：必须等 `B10` 语料数据基础就位后再启动

2. `voice.asr` / `voice.tts`
   - 当前未挂接真实记账
   - 当前第二波 issue 清单中**没有**单独 issue
   - 前提：供应商计量口径明确

3. `ai_cost_logs` 查询接口
   - 当前只有内部 `ListRecent(...)`
   - 当前第二波 issue 清单中**没有**单独 issue

4. `ai_cost_logs` 聚合统计
   - 当前无聚合表、无汇总口径
   - 当前第二波 issue 清单中**没有**单独 issue

## 推荐落位

1. `B10`
   - 不承接费用查询与统计
   - 只承接 `phrase_blocks` / `corpus`

2. `B12`
   - 承接 `voice.hit_detect`

3. 后续新增 residual / issue
   - `voice.asr / voice.tts` 记账
   - `ai_cost_logs` query API
   - `ai_cost_logs` aggregation/reporting
