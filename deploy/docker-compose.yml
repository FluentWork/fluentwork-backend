services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: fluentwork
      MYSQL_USER: fw
      MYSQL_PASSWORD: fw
      MYSQL_ROOT_PASSWORD: fwroot
    ports:
      - "3306:3306"
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_0900_ai_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-ufw", "-pfw"]
      interval: 2s
      timeout: 5s
      retries: 30
      start_period: 10s
